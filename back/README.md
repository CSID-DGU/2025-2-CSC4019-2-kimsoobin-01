# AI Idol English Teacher - Backend (FastAPI)

## Quick Start

```bash
cp .env.example .env
# .env에 GOOGLE_API_KEY / OPENAI_API_KEY / ELEVENLABS_API_KEY / VOICE_ID 입력

docker-compose up --build
# http://localhost:8000/health
